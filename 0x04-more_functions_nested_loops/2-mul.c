#include "main.h"
/**
 * mul - multiplies two integers
 * @a: first number
 * @b: second number
 * Return: multiplication of two the number
 */
int multi(int a, int b)
{
	int multi;

	multi = a * b;
	return (multi);
}

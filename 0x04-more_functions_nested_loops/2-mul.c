#include "main.h"
/**
 * mul - multiplies two integers
 * @a: first number
 * @b: second number
 * Return: multiplication of two the number
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
